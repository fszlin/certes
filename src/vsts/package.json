{
  "scripts": {
    "package": "npm run clean && npm run compile && tfx extension create",
    "gallery-publish": "tfx extension publish --rev-version",
    "clean": "rimraf ./*.vsix",
    "compile": "tsc -p .",
    "install-task-lib": "cd certes-ssl && npm install --save-dev",
    "postinstall": "cd certes-ssl && npm install"
  },
  "devDependencies": {
    "@types/node": "^8.0.7",
    "tfx-cli": "^0.4.5",
    "typescript": "2.6.2"
  },
  "name": "vstsexttask",
  "private": true,
  "version": "0.0.1"
}
