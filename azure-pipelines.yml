# Retired: CI is moving to GitHub Actions. The Azure build and release pipelines
# are disabled service-side. Keep explicit opt-outs to avoid implied CI if the
# build pipeline is accidentally re-enabled. See docs/ci-migration.md.
trigger: none
pr: none

jobs:
  - job: Retired
    condition: false
    pool:
      vmImage: ubuntu-latest
    steps:
      - checkout: none
