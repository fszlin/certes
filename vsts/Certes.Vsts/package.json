{
  "name": "certes-vsts",
  "description": "",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "nyc mocha",
    "nyc-html": "nyc mocha && nyc report --reporter=html",
    "nyc-lcov": "nyc mocha && nyc report --reporter=text-lcov > ../../build/coverage.lcov",
    "tfx-pack": "npm run clean && npm run install-tasks && npm run compile && tfx extension create -r ./ --output-path ../../build/",
    "clean": "rimraf ../../build/*.vsix",
    "compile": "tsc -p ./",
    "install-tasks": "npm run install-authz && npm run install-pfx",
    "install-authz": "cd dist/tasks/authz && npm install",
    "install-pfx": "cd dist/tasks/pfx && npm install"
  },
  "dependencies": {
    "string-template": "^1.0.0",
    "tldjs": "^2.2.0",
    "vsts-task-lib": "^2.1.0"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.44",
    "@types/node": "^8.0.57",
    "@types/q": "^1.0.6",
    "@types/string-template": "^1.0.2",
    "mocha": "^4.0.1",
    "nyc": "^11.4.1",
    "rimraf": "^2.6.2",
    "source-map-support": "^0.5.0",
    "tfx-cli": "^0.4.16",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fszlin/certes.git"
  },
  "nyc": {
    "include": [
      "tasks/*/*.ts"
    ],
    "extension": [
      ".ts",
      ".tsx"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "text"
    ],
    "all": true
  }
}
